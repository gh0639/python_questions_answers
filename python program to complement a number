def FindComplement(num):
  binary=bin(num)[2:]
  complement=''
  for bit in binary:
      if bit =='1':
         complement+='0'
      else:
         complement+='1'
  return int(complement, 2)

n=int(input("enter a number: "))
print(FindComplement(n))
  
